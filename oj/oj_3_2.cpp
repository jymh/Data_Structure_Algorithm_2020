#include <bits/stdc++.h>
using namespace std;

struct node {
    int index;
    int value;
}

int main() {
    int t;
    scanf("%d", &t);
    for(int i = 0; i < t; i++) {
        scanf("%d", )
    }
}